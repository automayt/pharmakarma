    <div id="tickerName"></div>

    <script>
    d3.json('data/tickerNamestock.json', function(data) {
        data = MG.convert.date(data, 'date');
        var markers = [{
            'date': new Date('labelTypeDate'),
            'label': 'labelType'
        }];
        MG.data_graphic({
            title: "Confidence Band",
            description: "This is an example of a graphic with a confidence band and extended x-axis ticks enabled.",
            data: data,
            width: 600,
            height: 200,
            right: 40,
            area: false,
            target: '#tickerName',
            show_secondary_x_label: false,
            show_confidence_band: ['low', 'high'],
            y_accessor: ['close', 'low', 'high'],
            legend: ['close','low','high'],
            legend_target: '.legend',
            x_extended_ticks: true,
            animate_on_load: true,
            missing_is_hidden: true,
            markers: markers,
            missing_is_hidden_accessor: true
        });
    });

    </script>

